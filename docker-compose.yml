version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: analysisapp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: analysis_app
      POSTGRES_USER: analysis_user
      POSTGRES_PASSWORD: analysis_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U analysis_user -d analysis_app"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
